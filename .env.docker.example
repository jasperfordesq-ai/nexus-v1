# =============================================================================
# Project NEXUS - Docker Environment Configuration
# =============================================================================
# Copy this file to .env.docker and fill in your own values.
# NEVER commit .env.docker — it is gitignored.
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DB_TYPE=mysql
DB_HOST=db
DB_PORT=3306
DB_NAME=nexus
DB_USER=nexus
DB_PASS=change_me
DB_ROOT_PASSWORD=change_me

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8090
APP_NAME="Project NEXUS"
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# API Keys (Optional - fill in your own)
# -----------------------------------------------------------------------------
# Pusher (real-time notifications) — https://pusher.com/
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_ID=
PUSHER_APP_CLUSTER=eu

# OpenAI (AI features) — https://platform.openai.com/
OPENAI_API_KEY=

# Google Maps (Places Autocomplete + Geocoding)
# Get keys from https://console.cloud.google.com/ (enable Places API + Geocoding API)
GOOGLE_MAPS_API_KEY=
GOOGLE_GEOCODING_API_KEY=

# -----------------------------------------------------------------------------
# Email (for local development, use Mailhog or leave empty)
# -----------------------------------------------------------------------------
MAIL_DRIVER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_FROM_ADDRESS=noreply@localhost
MAIL_FROM_NAME="NEXUS Local"
