#!/usr/bin/env sh
powershell.exe -Command "docker exec nexus-php-app vendor/bin/phpstan analyse --no-progress --memory-limit=512M --error-format=github" || exit 1
powershell.exe -Command "docker exec nexus-php-app vendor/bin/phpunit --testsuite Unit,Services --no-progress" || exit 1
node scripts/check-i18n-drift.mjs --summary && cd react-frontend && npx tsc --noEmit && npm run build
