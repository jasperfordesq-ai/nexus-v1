# Federation Coverage Verified

Generated by `tests/Laravel/Feature/Federation/*` — run:

```bash
docker exec nexus-php-app vendor/bin/phpunit \
  --filter 'TwoWayFlow|KomunitinProtocol|CreditCommonsProtocol|NativeNexus|TimeOverflow|CrossProtocol'
```

Last verified: 2026-04-12. Result: **118 tests, 171 assertions, 93 passed, 25 incomplete, 1 skipped, 0 failed.**

A cell is ticked only when the corresponding `TwoWayFlowTest` data-provider row actually passes. ❌ means the test is marked incomplete pending infrastructure.

## Matrix

| Entity          | Nexus OUT | Nexus IN | Komunitin OUT | Komunitin IN | CC OUT | CC IN | TO OUT | TO IN |
| --------------- | --------- | -------- | ------------- | ------------ | ------ | ----- | ------ | ----- |
| Listing         | ✅        | ❌ [1]   | ✅            | ❌ [1]       | ✅     | ❌ [1] | ✅     | ❌ [1] |
| Message         | ✅        | ✅       | ✅            | ✅           | ✅     | ✅    | ✅     | ✅    |
| Transaction     | ✅        | ✅       | ✅            | ✅           | ✅     | ✅    | ✅     | ✅    |
| Review          | ✅        | ❌ [2]   | ✅            | ❌ [2]       | ✅     | ❌ [2] | ✅     | ❌ [2] |
| CommunityEvent  | ✅        | ❌ [3]   | ✅            | ❌ [3]       | ✅     | ❌ [3] | ✅     | ❌ [3] |
| Group           | ✅        | ❌ [4]   | ✅            | ❌ [4]       | ✅     | ❌ [4] | ✅     | ❌ [4] |
| Connection      | ✅        | ❌ [5]   | ✅            | ❌ [5]       | ✅     | ❌ [5] | ✅     | ❌ [5] |
| Volunteering    | ✅        | ❌ [6]   | ✅            | ❌ [6]       | ✅     | ❌ [6] | ✅     | ❌ [6] |
| Member          | ✅        | ✅       | ✅            | ✅           | ✅     | ✅    | ✅     | ✅    |

## Footnotes — incomplete cells

All missing cells are inbound-webhook handlers. The adapters correctly normalize the
wire payload, but `FederationExternalWebhookController::handleEvent()` has no branch
for the following event types — they fall through to `['status' => 'unhandled']`:

- [1] `listing.created` — needs `handleInboundListing()` + `FederatedListingReceived` dispatch + shadow `federation_listings` upsert.
- [2] `review.created` — needs `handleInboundReview()` + `FederatedReviewReceived` event + shadow `federation_reviews` table.
- [3] `event.created` — needs `handleInboundCommunityEvent()` + `FederatedCommunityEventReceived` event + shadow `federation_events` table.
- [4] `group.created` — needs `handleInboundGroup()` + `FederatedGroupReceived` event + shadow `federation_groups` table.
- [5] `connection.requested` — needs `handleInboundConnection()` + `FederatedConnectionReceived` event + persist to `federation_connections`.
- [6] `volunteering.created` — needs `handleInboundVolunteering()` + `FederatedVolunteeringReceived` event + shadow `federation_volunteering` table.

## Protocol-specific deep tests

| Test                                      | Status              |
| ----------------------------------------- | ------------------- |
| `KomunitinProtocolTest` (5 tests)         | ✅ all pass         |
| `CreditCommonsProtocolTest` (4/5 pass)    | ⚠️ hashchain validation `validateHashchain()` not yet on adapter |
| `NativeNexusProtocolTest` (7 tests)       | ✅ all ingest POST routes registered |
| `TimeOverflowProtocolTest` (3 tests)      | ✅ all pass         |
| `CrossProtocolRegressionTest` (2 tests)   | ✅ all pass         |

## Outbound listener coverage

All 9 outbound listeners are present on `main`:

- `PushListingToFederatedPartners`
- `PushMessageToFederatedPartner`
- `PushTransactionToFederatedPartner`
- `PushReviewToFederatedPartner`
- `PushCommunityEventToFederatedPartners`
- `PushGroupToFederatedPartners`
- `PushConnectionAcceptedToFederatedPartner`
- `PushVolunteerOpportunityToFederatedPartners`
- `PushMemberProfileUpdateToFederatedPartners`

They all successfully resolve a per-partner adapter and issue a POST to the partner
base URL (verified with `Http::fake()` + `Http::assertSent()`).
