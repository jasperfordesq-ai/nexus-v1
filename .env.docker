# =============================================================================
# Project NEXUS - Docker Environment Configuration
# =============================================================================
# This file is the source of truth for Docker Compose environment variables.
# Copy to .env.docker.local for local overrides (gitignored).
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# These connect to the Docker MySQL container (service name: db)
DB_TYPE=mysql
DB_HOST=db
DB_PORT=3306
DB_NAME=nexus
DB_USER=nexus
DB_PASS=nexus_secret
DB_ROOT_PASSWORD=nexus_root_secret

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8090
APP_NAME="Project NEXUS"

# -----------------------------------------------------------------------------
# API Keys (Optional - fill in your own)
# -----------------------------------------------------------------------------
# Pusher (real-time notifications)
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_ID=
PUSHER_APP_CLUSTER=eu

# OpenAI (AI features)
OPENAI_API_KEY=

# Google Maps (Places Autocomplete + Geocoding)
GOOGLE_MAPS_API_KEY=

# -----------------------------------------------------------------------------
# Email (for local development, use Mailhog or leave empty)
# -----------------------------------------------------------------------------
MAIL_DRIVER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_FROM_ADDRESS=noreply@localhost
MAIL_FROM_NAME="NEXUS Local"
